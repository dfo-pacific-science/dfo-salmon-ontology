repos:
  - repo: local
    hooks:
      - id: ontology-ci
        name: Run full ontology CI bundle (make ci)
        entry: devenv shell make ci
        language: system
        files: ^ontology/dfo-salmon\\.ttl$
        pass_filenames: false
        always_run: false






